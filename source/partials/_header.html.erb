<header data-controller="navbar">
  <div class="container">
    <div class="covid-message">
      <div class="content is-medium">
        <h4>COVID 19 Model Home Policy</h4>
        <ul>
          <li>Social distancing.</li>
          <li>Masks available to those having none.</li>
          <li>Only 1 family at a time may tour model.</li>
          <li>Guests may wash & sanitize hands at departure.</li>
          <li>Surfaces sanitized after each visit.</li>
        </ul>
      </div>
    </div>
  </div>
  <nav class="navbar has-shadow">
    <div class="container">
      <div class="navbar-brand">
        <% link_to '/', class: 'navbar-item' do %>
          <%= image_tag data.settings.logo %>
        <% end %>
        <a
          role="button"
          class="navbar-burger"
          aria-label="menu"
          aria-expanded="false"
          data-target="navbar.button"
          data-action="navbar#toggleMenu"
        >
          <span aria-hidden="true"></span>
          <span aria-hidden="true"></span>
          <span aria-hidden="true"></span>
        </a>
      </div>
      <div class="navbar-menu" data-target="navbar.menu">
        <div class="navbar-end">
          <% data.navigation.links.each do |link| %>
            <%= link_to link.label, link.url, class: 'navbar-item is-size-4' %>
          <% end %>
        </div>
      </div>
    </div>
  </nav>
</header>
