<header>
  <% if section.title.present? %>
    <p class="title is-1"><%= section.title %></p>
  <% end %>
  <% if  section.subtitle.present? %>
    <p class="subtitle is-3"><%= section.subtitle %></p>
  <% end %>
  <hr class="is-invisible" />
</header>
